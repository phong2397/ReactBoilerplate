/*
 * OrderInfo Messages
 *
 * This contains all the text for the OrderInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderInfo component!',
  },
});
