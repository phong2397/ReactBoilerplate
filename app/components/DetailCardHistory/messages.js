/*
 * DetailCardHistory Messages
 *
 * This contains all the text for the DetailCardHistory component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DetailCardHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailCardHistory component!',
  },
});
