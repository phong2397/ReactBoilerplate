/*
 * TabPanel Messages
 *
 * This contains all the text for the TabPanel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TabPanel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TabPanel component!',
  },
});
