/*
 * OrderAppraiseStatus Messages
 *
 * This contains all the text for the OrderAppraiseStatus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderAppraiseStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderAppraiseStatus component!',
  },
});
