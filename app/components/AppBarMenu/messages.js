/*
 * AppBarMenu Messages
 *
 * This contains all the text for the AppBarMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AppBarMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppBarMenu component!',
  },
});
