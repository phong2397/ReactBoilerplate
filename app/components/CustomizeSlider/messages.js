/*
 * CustomizeSlider Messages
 *
 * This contains all the text for the CustomizeSlider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomizeSlider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomizeSlider component!',
  },
});
