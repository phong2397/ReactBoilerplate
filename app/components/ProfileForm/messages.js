/*
 * ProfileForm Messages
 *
 * This contains all the text for the ProfileForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProfileForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileForm component!',
  },
});
