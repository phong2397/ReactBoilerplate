/*
 * ModalSuccess Messages
 *
 * This contains all the text for the ModalSuccess component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ModalSuccess';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModalSuccess component!',
  },
});
