/*
 * ListDocument Messages
 *
 * This contains all the text for the ListDocument component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ListDocument';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListDocument component!',
  },
});
