/*
 * BankCard Messages
 *
 * This contains all the text for the BankCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BankCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BankCard component!',
  },
});
