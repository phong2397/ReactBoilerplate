/*
 * AppbarDetail Messages
 *
 * This contains all the text for the AppbarDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AppbarDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppbarDetail component!',
  },
});
