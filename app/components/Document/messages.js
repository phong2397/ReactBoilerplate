/*
 * Document Messages
 *
 * This contains all the text for the Document component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Document';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Document component!',
  },
});
