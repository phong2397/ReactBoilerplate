/*
 * OrderDisbursementStatus Messages
 *
 * This contains all the text for the OrderDisbursementStatus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderDisbursementStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderDisbursementStatus component!',
  },
});
