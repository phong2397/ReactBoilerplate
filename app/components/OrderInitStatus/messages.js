/*
 * OrderInitStatus Messages
 *
 * This contains all the text for the OrderInitStatus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderInitStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderInitStatus component!',
  },
});
