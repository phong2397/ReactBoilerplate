/*
 * OrderDetailPage Messages
 *
 * This contains all the text for the OrderDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderDetailPage container!',
  },
});
