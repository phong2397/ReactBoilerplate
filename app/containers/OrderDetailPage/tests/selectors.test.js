// import { selectOrderDetailPageDomain } from '../selectors';

describe('selectOrderDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
