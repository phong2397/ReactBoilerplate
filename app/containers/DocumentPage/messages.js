/*
 * DocumentPage Messages
 *
 * This contains all the text for the DocumentPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DocumentPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DocumentPage container!',
  },
});
