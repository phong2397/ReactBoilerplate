/*
 * RequestOrderPage Messages
 *
 * This contains all the text for the RequestOrderPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestOrderPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestOrderPage container!',
  },
});
