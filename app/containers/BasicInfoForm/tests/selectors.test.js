// import { selectBasicInfoFormDomain } from '../selectors';

describe('selectBasicInfoFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
