/*
 * ProfileInfoPage Messages
 *
 * This contains all the text for the ProfileInfoPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProfileInfoPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileInfoPage container!',
  },
});
