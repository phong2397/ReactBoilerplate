// import { selectCompletedOrderDomain } from '../selectors';

describe('selectCompletedOrderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
