// import { selectIntroducePageDomain } from '../selectors';

describe('selectIntroducePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
